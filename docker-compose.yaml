services:
  # Сервис PostgreSQL
  postgres:
    image: postgres:15.6
    restart: always
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_tcms_storage:/var/lib/postgresql/data
    env_file:
      - .env
    networks:
      - tcms
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}" ]
      interval: 5s
      timeout: 5s
      retries: 5
    container_name: tcms_postgres

  # Сервис FastAPI
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - tcms
    env_file:
      - .env
    container_name: tcms_backend

volumes:
  postgres_tcms_storage:

networks:
  tcms: